import { UsersSocials } from "users-socials"

model Socials {
    id     Int    @id @default(autoincrement())
    name   String
    status String

    UsersSocials UsersSocials[]

    @@map("socials")
}
